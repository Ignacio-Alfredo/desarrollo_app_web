console.log("Contectado correctamente...")

function promedioTresPrimeros(){
    let numeros = [6, 4, 5, 7, 2];
    let promedio = numeros[0]+numeros[1]+numeros[2]/3;
    alert("El promedio de los 3 primeros numeros es: \n"
        + promedio);
}