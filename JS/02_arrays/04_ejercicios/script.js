console.log("Conectado correctamente...")

function multiplicaPares() {
    let numeros = [4,6,2,10,8];
    let multiplicacion = (numeros[0]*numeros[2]*numeros[4]);
    alert("La multiplicacion de las posiciones pares tiene un valor de: \n" + multiplicacion);
}