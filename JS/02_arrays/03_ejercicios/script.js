console.log("Conectado correctamente...")

function sumaExtremos(){
    let numeros = [4, 3, 6, 5, 1];
    let resultado = numeros[0] + numeros[numeros.length -1];

alert("El resultado de la suma es: \n"
    + resultado)
    
}